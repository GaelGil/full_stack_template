// movie type for declaring var types
export type User = {
  id: number;
  username: string;
  email: string;
};

// moviecardProps type for declaring var types
export type UserProps = {
  user: User;
};
